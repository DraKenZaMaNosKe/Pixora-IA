# v1.7.15+58 — 2026-05-14

## English (<500 chars)

```
NEW: Curated rotation in Pixora Daily — admin marks wallpapers as "eligible" and only those rotate. No more random mixes of calendar + anime.

NEW: Sections by tags — Arte and Mitología sections show wallpapers across categories. A panoramic with tag "arte" appears in both Panoramic AND Arte sections.

NEW: 2 categories added (MITOLOGIA, DAILY) — total 31.

Bug fix: bottom sheet overflow in Pixora Daily category picker (74px clipped).
```

## Español (<500 chars)

```
NUEVO: Rotación curada en Pixora Daily — el admin marca wallpapers como "eligibles" y solo esos rotan. Adiós mezclas raras de calendario + anime.

NUEVO: Secciones por tags — Arte y Mitología muestran wallpapers cross-categoría. Un panorámico con tag "arte" aparece en Panoramic Y en Arte al mismo tiempo.

NUEVO: 2 categorías más (MITOLOGIA, DAILY) — total 31.

Fix: overflow del bottom sheet en el picker de Pixora Daily (74px cortados).
```

## Commits desde v1.7.14

- `e8c1d11` feat(catalog): daily-eligible flag + Arte/Mitologia sections + UI overflow fix
- `cd68352` fix(dashboard): dropdown completo con las 28 categorias reales del enum

## Cambios técnicos

### Database
- `wallpapers.daily_eligible boolean DEFAULT false` + partial index
- `wallpapers_v` view recreada exponiendo el flag
- Enum `wallpaper_category`: +DAILY +CALENDARIOS +MITOLOGIA (31 valores total). DAILY/CALENDARIOS quedan como flags-dormant (no usadas como sección)

### App (Flutter)
- `Wallpaper.dailyEligible` campo nuevo en el modelo
- `AutoRotateService`: category='DAILY' → filtra por `dailyEligible` boolean en vez de category match. Conserva categoría natural del wallpaper.
- `wallpapers_page.dart`: 2 SliverToBoxAdapter nuevos entre New y Category rows — _ArteRow + _MitologiaRow
- `wallpaper_providers.dart`: 2 providers nuevos filtran por tag `arte`/`mitologia` o categoría match. `_curatedSectionCategories` skip en `_CategoryRows` para no duplicar
- `pixora_daily_page.dart`: bottom sheet con Flexible+SingleChildScrollView+ConstrainedBox(maxHeight: 75%). isScrollControlled. Picker añade "Pixora Daily (curado)" y "Calendarios"

### Dashboard (Pixora Admin)
- Toggle "★ Eligible para Pixora Daily" en edit modal
- Badge azul-celeste estrella en cards daily-eligible
- Lightbox para ver imagen full-res (click en preview del modal)
- Hint de dimensiones + warning rojo si admin elige categoría incompatible (PANORAMIC en 9:16, AURA en no-audio)
- Server: campo `dailyEligible` en `_STATIC_FIELD_MAP`, enrichment con daily_eligible

## Notes

- Track: **Closed Testing** (segmento "prueba_cerrada")
- AdMob: TEST AD ID activo hasta Production
- AAB SHA-1: FF:0F:46:D4:E2:86:25:D1:14:C6:81:03:11:E4:6B:E4:47:2A:CD:79
- Convención tags-sección: lowercase sin acentos (`arte`, `mitologia`, `daily`)
