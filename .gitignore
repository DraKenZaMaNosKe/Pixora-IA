# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# Legal documents — filled with PII (RFC, full address, full name).
# Templates with placeholders ARE tracked in docs/legal/_TEMPLATES/.
# Don't commit the populated .md files; they live local-only until uploaded
# to Supabase Storage in their final HTML form.
docs/legal/terms_v1.md
docs/legal/privacy_v1.md
docs/legal/terms_v*.md
docs/legal/privacy_v*.md
!docs/legal/_TEMPLATES/

# Admin tools runtime artefacts (local logs, ad-hoc previews, test caps)
tools/wallpapers/admin_server.log
tools/wallpapers/*.local.js
docs/design/_device_*.png
docs/design/admin_dashboard_test.png

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Environment & Keys
.env
*.env.local
*.jks
*.keystore
android/key.properties
google-services.json
GoogleService-Info.plist
KEYS_LOCAL.md

# Master doc snapshots (local only, not for version control)
docs/master_doc_snapshots/

# Claude Code — local settings with user-specific paths and device serials
.claude/settings.local.json
.claude/settings.local.json.bak
.claude/*.bak

# Master doc snapshots (private, sometimes large)
docs/master_doc_snapshots/

# Batch content generation jobs (huge, temporary)
tools/batch/

# Sprite originals (33 MB) — backed up locally, regenerable from prompts
docs/design/concepts/iah_statues/_originals/

# Dev scripts archive — sync to orbixprivate instead
scripts/archive/

# Deploy payloads — may contain Google Play SA private key, NEVER commit
.deploy_payload.json
*.deploy_payload.json

# Generated Flutter platform files (regenerated by `flutter pub get`)
linux/flutter/generated_plugins.cmake
linux/flutter/ephemeral/
macos/Flutter/GeneratedPluginRegistrant.swift
macos/Flutter/ephemeral/

# Local Android build output
/android/build/

# Claude scheduled tasks lock
.claude/scheduled_tasks.lock

# Throwaway temp files at repo root (screenshots, exports, scratch)
/tmp_*

# Per-feature upload staging directories (tools/wallpapers/_tmp_*, tools/ringtones/_tmp_*)
# Often contain ad-hoc scripts with secrets pulled from KEYS_LOCAL.md
_tmp_*/

# AI content production staging — uploads to Supabase Storage, not git
/wallpapers_new/

# Master doc HTML mirrors — contain OAuth/Google secrets, NEVER commit
/docs/master_doc/
/docs/design/master_doc.html

# Firebase service account (FCM admin)
firebase-service-account.json
/android/app/build/

# Local caches / experimental sandboxes — no repo value
/agent-tools/
/mcps/
/terminals/
/rel
_cstmp/
tools/rig/_cstmp/
tools/rig/_pushtmp/
supabase/.temp/
.codex/
.agents/

# Key.properties backups — contain keystore passwords, NEVER commit
android/key.properties.bak*
# Runtime publish-progress state — ephemeral, no repo value
tools/wallpapers/dashboard/batch_publish_progress.json
